name: Docker Image CI

on: push

jobs:

  build:
  
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image for Ubuntu
      run: |
        cd build-linux
        docker build -t build_for_ubuntu:$(date +%s) .
